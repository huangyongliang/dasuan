# 大蒜助手 (Dasuan Helper) 开发完成计划

## 1. 项目初始化
- [x] 创建 `project.config.json`, `app.json`, `app.js`
- [x] 配置云开发环境

## 2. 数据库设计
- [x] 设计 `notes` 集合结构 (详见 README.md)
- [x] 实现用户隔离 (基于 `_openid`)

## 3. 功能实现
- [x] **笔记列表页 (pages/index)**: 展示用户自己的笔记，按时间倒序。
- [x] **编辑页 (pages/editor)**: 
    - 支持 Markdown 文本输入
    - 支持图片上传 (存储到云存储)
    - 支持存草稿 / 提交审核
- [x] **个人/管理页 (pages/profile)**:
    - 展示用户信息
    - 管理员模式开关 (模拟/演示用)
    - 待审核列表 (调用云函数获取所有人的待审核笔记)
    - 审核操作 (通过/驳回)

## 4. 后端逻辑 (云函数)
- [x] 创建 `admin` 云函数
    - `getPending`: 获取所有待审核笔记 (突破前端权限限制)
    - `audit`: 更新笔记状态

## 后续操作建议 (用户需执行)
1. 在微信开发者工具中打开项目。
2. 开通云开发环境。
3. 在云开发控制台 -> 数据库中创建 `notes` 集合。
4. 在云开发控制台 -> 云函数中上传部署 `admin` 云函数。
